apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-sentinel.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kube-sentinel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-sentinel.selectorLabels" . | nindent 4 }}
