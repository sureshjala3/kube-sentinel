services:
  kube-sentinel:
    build: .
    image: kube-sentinel:local
    ports:
      - "8080:8080"
    environment:
      - PORT=${PORT:-8080}
      #- HOST=${HOST}
      #- CLOUD_SENTINEL_K8S_BASE=${CLOUD_SENTINEL_K8S_BASE}
      #- JWT_SECRET=${JWT_SECRET}
      #- CLOUD_SENTINEL_K8S_ENCRYPT_KEY=${CLOUD_SENTINEL_K8S_ENCRYPT_KEY}
      #- CLOUD_SENTINEL_K8S_USERNAME=${CLOUD_SENTINEL_K8S_USERNAME:-admin}
      #- CLOUD_SENTINEL_K8S_PASSWORD=${CLOUD_SENTINEL_K8S_PASSWORD:-admin}
      - DB_TYPE=${DB_TYPE}
      - DB_DSN=${DB_DSN}
      #- KUBECONFIG=${KUBECONFIG}
      #- NODE_TERMINAL_IMAGE=${NODE_TERMINAL_IMAGE}
      - GITLAB_HOSTS=${GITLAB_HOSTS}
      - INSECURE_SKIP_VERIFY=${INSECURE_SKIP_VERIFY}
      #- DISABLE_GZIP=${DISABLE_GZIP}
      #- DISABLE_VERSION_CHECK=${DISABLE_VERSION_CHECK}
