apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kube-sentinel
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
  - kind: ServiceAccount
    name: kube-sentinel
    namespace: kube-system
---
kind: ServiceAccount
apiVersion: v1
metadata:
  name: kube-sentinel
  namespace: kube-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: kube-sentinel
  name: kube-sentinel
  namespace: kube-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kube-sentinel
  strategy: {}
  template:
    metadata:
      labels:
        app: kube-sentinel
    spec:
      serviceAccountName: kube-sentinel
      containers:
        - image: ghcr.io/pixelvide/kube-sentinel:latest
          imagePullPolicy: IfNotPresent
          name: kube-sentinel
          resources:
            limits:
              cpu: 1000m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          ports:
            - containerPort: 8080
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: kube-sentinel
  name: kube-sentinel
  namespace: kube-system
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: kube-sentinel
  type: ClusterIP
